

import 'package:custom_recurring_selectors/enum/frequency.dart';

class FrequencyGeneratorUtil {
  static List<String> generate(Frequency frequency) {
    if (frequency == Frequency.DAILY) {
      return generateDailyFrequency();
    }
    else if (frequency == Frequency.DAILY) {
      return generateW
    }
  }
}